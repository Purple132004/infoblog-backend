```
npm install
npm run dev
```

```
cp .env.example .env
```

Aggiorna `DATABASE_URL` nel file `.env` con le credenziali reali del tuo Postgres.

```
npx drizzle-kit studio
```

```
open http://localhost:3000
```
